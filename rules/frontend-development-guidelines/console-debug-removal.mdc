---
description: Management and removal of debug console statements.
globs: "**/*.{js,ts,jsx,tsx,vue}"
---
# Console Debug Removal

- **No Consoles**: Remove all `console.log/debug` before committing.
- **Explicit Exceptions**: If a log is necessary for production, use `// eslint-disable-next-line no-console` to justify it.