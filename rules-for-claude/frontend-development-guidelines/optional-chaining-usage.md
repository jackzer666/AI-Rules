---
description: Proper usage of optional chaining (?.) and avoiding redundancy.
globs: "**/*.{js,ts,jsx,tsx}"
---
# Optional Chaining Logic

- Use `?.` to access deeply nested properties.
- If `a?.b` already checked in `if`, use `a.b` directly inside the block.