---
description: Standards for magic literals, constants, and storage keys.
paths:
  - "**/*.{js,ts,jsx,tsx}"
---
# Constants & Magic Literals

- Compare against named constants (e.g., `STATUS_SUCCESS`), not magic values.
- `localStorage` keys: `UPPER_SNAKE_CASE`, placed at file top.